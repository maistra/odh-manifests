apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: odh-dashboard
  namespace: opendatahub
spec:
  gateways:
  - odh-gateway
  hosts:
  - "*"
  http:
  - route:
    - destination:
        host: odh-dashboard
        port:
          number: 80
