apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: init-job-role
# We have this role/binding for our init-job to create authconfig
# in the KfDef namespace.
rules:
  - apiGroups:
      - authorino.kuadrant.io
    resources:
      - authconfigs
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch